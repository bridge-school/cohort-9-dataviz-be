// STRETCH GOAL

const request = require("supertest");

const { cohortsRouter } = require("./cohorts.router");

describe("tests for the cohorts endpoint", () => {
  it("returns a 200", () => {
    expect(true).toBe(true);
  });
});